<p id="notice"><%= notice %></p>

<h2>
  <%= @list.name %> <span style="font-size:10px"><%= link_to 'Edit', edit_list_path(@list) %></span>
</h2>

<p>
  <%= @list.description %>
</p>

<ul class="items-ul">
  <% @items.each do |i| %>
    <li class="items-li">
      <span><%= i.name %></span> <span class="quantity-span">x<%= i.quantity %></span>
    </li>
  <% end %>
</ul>

<%= link_to 'Add another item', new_item_path(list: @list.id) %>
</br>
</br>
<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
